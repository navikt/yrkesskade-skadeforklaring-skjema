{
  "name": "test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "cypress-file-upload": "^5.0.8"
  },
  "devDependencies": {
    "@cypress/code-coverage": "^3.9.12",
    "@cypress/instrument-cra": "^1.4.0",
    "cypress": "^9.3.1",
    "dayjs": "^1.10.7"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:open:local": "cypress open --env local=true",
    "cypress:run:local": "cypress run --env local=true",
    "build-prod": "cd .. && yarn build-prod",
    "dev": "cd .. && yarn dev"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
