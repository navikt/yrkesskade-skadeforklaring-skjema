import { PageHeader } from '@navikt/ds-react';
import {} from '@navikt/ds-icons';

const SystemHeader = () => {
  return (
    <PageHeader variant="product" description="SKJEMA" className="no-print">
      Skadeforklaringskjema
    </PageHeader>
  );
};

export default SystemHeader;
